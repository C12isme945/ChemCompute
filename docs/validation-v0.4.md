# v0.4.0 验收记录（2026-09-12）

## 已验证

- 完整测试 143 passed、1 skipped；随后增加并通过运行互斥锁测试 1 项。跳过项为 Windows 符号链接权限。
- 更新模块 19 项：严格版本比较、预发布通道、拒绝缺少摘要、受限 HTTPS 跳转、下载取消、损坏/超大文件拒绝、保留先前文件。
- 升级维护锁 14 项：所有记录检查、排队/运行/恢复拒绝、未知状态拒绝、未回传任务拒绝、与新任务提交互斥、锁过期和令牌隔离。
- 原有鉴权、计算包、Gaussian、DeepSeek、节点与检查点恢复测试恢复；未经鉴权的替代管理/插件更新路由移出活动程序，Git 历史保留。
- 实际 Tk 桌面七页控件、启动/重复启动/重启/停止通过；节点概览和更新页截图已检查，底部操作可见，原有表单支持滚动。
- 真实 WSL GROMACS 上传计算包、grompp/mdrun、结果下载与 SHA-256 校验通过。
- 真实主控断线、代理重启、GROMACS 检查点续算与结果补传通过。
- 真实隔离安装升级：旧版0.3启动、独立PowerShell助手、任务维护锁、安装0.4、后台及桌面重启、管理员鉴权、配置与结果逐字节保留均通过。证据目录名 `chemcompute-upgrade-hwf5xvfm`（本机临时目录）。
- 发现旧版随机 TCP 单实例端口被 Windows 禁止绑定（10013），新版改用 Windows 命名互斥锁；Linux 使用文件锁。
- Ruff 检查通过；保留阻塞 lint 的 Linux/Windows CI、独立程序与安装/卸载测试。

## 限制

在线升级需要短暂重启，纯计算节点仍需手动安装；不提供内存中代码替换、自动回滚、跨节点永久故障迁移或单任务 MPI/Linda。安装包未签名。实际 Gaussian 仍需用户授权安装，未进行真实 Gaussian 计算。全新电脑上的 WSL/驱动安装和重启流程仍待验收。

## 委派验收边界

兼容性恢复提交 `9be438f` 经本地测试验收。AGPair `20260912084349-4e3afcab`、`20260912084353-35954435` 和缩小范围的重试 `20260912085350-a3cede2f` 未通过验收，未应用补丁；在线更新和界面由主控本地实现并验证。初始广泛任务 `20260912083612-ed5a8c7c` 在发现核心回退后取消。

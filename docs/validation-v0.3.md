# v0.3.0 验证记录

验证日期：2026-09-12。Windows x64 本机。

## 已实测

- Ruff 通过；完整测试：101 passed，1 skipped（Windows 符号链接权限）。
- PyInstaller 独立程序与 Inno Setup 安装包构建成功。
- 打包程序启动主控、注册节点、上报心跳、鉴权读取节点列表。
- 六页原生桌面控件及事件循环、后台启动/重复启动/重启/停止。使用隐藏窗口冒烟检查，未做人工视觉验收。
- 独立安装目录：安装、自动桌面快捷方式、初始化、安装后程序和操控台、卸载、保留配置。
- 真实 WSL GROMACS：上传两水分子计算包，预处理、100 步计算、下载结果及 SHA-256 校验。
- 真实 WSL GROMACS 恢复：生成检查点后停止主控和代理，重新启动，确认从 `run.cpt` 继续而非从零开始，完成结果回传。由 `scripts/smoke_reconnect.py --exe dist/ChemCompute/ChemCompute.exe` 验证。
- 两个逻辑节点并发领取独立副本、租约失效、原节点恢复、拒绝旧令牌、网络中断后不重复已完成步骤。
- Gaussian 模拟执行、输入安全边界、正常结束标志、超时及取消测试。
- 可选依赖安装脚本语法和 PlanOnly 检查；未在此测试中修改本机驱动。

## 尚未验证或未实现

- 尚未在另一台完全无 Python 的实体电脑完成新装；打包测试清除了 Python 环境依赖，安装包包含解释器。
- WSL/Ubuntu/GROMACS/驱动的全新系统安装及重启后恢复尚待实机验收。依赖需要网络、磁盘空间，可能需要管理员权限。
- 未运行真实 Gaussian；用户须自行提供已授权安装，探测成功不证明许可证可用。安装包不分发 Gaussian。
- 跨机调度使用独立任务；不支持单个任务跨互联网 MPI/Linda 并行。相同输入的副本不会自动修改随机种子。
- 失联保持原节点所有权，支持重连和同节点 GROMACS 检查点恢复；不提供永久节点故障后的自动迁移。
- 本机主控需要电脑开机，登录后自启；不是已验证的无人登录 Windows 服务部署。安装包未代码签名，属于预发布。

## 委派与验收

Antigravity 接受的独立执行记录：恢复模块 `20260911151157-1ef93abe`（8 个测试）；Gaussian adapter `20260911151358-c3594f08`（23 个测试）。此前 lint 未通过的执行未应用；重试通过后应用，并由主控进一步修订和完成整套验收。

GitHub Actions 持续执行 Linux lint/test、Windows test/build、独立程序与安装卸载检查。真实 GROMACS 故障恢复需可用的本地 GROMACS 环境，不在通用 GitHub runner 上冒充执行。

# v1.0.0 验收记录

2026-09-13 Windows 本地验收：

- Ruff 通过；pytest 195 passed, 1 skipped。
- 28 项新增算力与 UI 测试（含原生进程单核心亲和性、Gaussian 测试替身、暂停不接单、断线保持预算、超额拒绝、桌面保存读取）。没有以测试替身冒充科学计算。
- PyInstaller 内置 Python/Tk、Inno Setup 编译成功。
- 主程序及安装器 SHA-256 自签名；CMS 验证通过，Windows 报根证书不受信任。篡改副本返回 HashMismatch。
- 打包后的主控、注册、节点心跳、认证、原生窗口、启动/重启/停止检查通过。
- 独立测试实例完成真实 0.6.0 → 1.0.0 安装升级、后台重启、认证和配置/结果保留。
- 界面已截图检查，算力页支持滚动且保存按钮固定。截图为隔离预览，不代表在线节点数量。

尚未验证：不同物理电脑的吞吐量、真实 Gaussian 计算、当前故障 WSL 上的 GROMACS 计算及 taskset 实机执行、无人登录启动。内存是调度预算，不是硬限额。在线升级需要空闲并短暂重启。

委托记录：20260913050824-142b705e 和 20260913051521-f2b2d29a 未通过验收，未直接应用；最终由本地实现补齐执行路径与测试。

# v0.2.0 验证记录

时间：2026-09-11。Windows 本机验证；不等价于第二台全新电脑验收。

- ruff：通过。
- pytest：67 passed，1 skipped（当前账号没有符号链接创建权限）。
- 打包程序：在移除外部 Python PATH/PYTHONHOME/PYTHONPATH 后，首次配置、控制端启动、注册、心跳、管理员认证资产清单通过。
- 原生 Tk：5 个页签、组件构建、事件循环、状态渲染及关闭检查通过；未声称人工视觉验收。
- 桌面后台生命周期：启动、重复启动、状态、关窗保留后台、重启、停止通过。
- 安装器：隔离安装、自动桌面快捷方式、配置生成、安装后的程序与 Tk、卸载及保留配置通过。
- 实际 WSL GROMACS：两水分子 100 步 CPU 示例输入上传、任务领取、grompp + mdrun、结果上传/下载和 SHA-256 一致性通过。源码与冻结程序各完成测试。不证明科学体系有效性。
- DeepSeek：当前账号完成真实模型列表请求和一次合成任务节点建议；API 密钥只在本机 DPAPI 存储，不在源码、安装包、测试或发布附件中。
- 计算包：路径穿越、原始反斜杠、Windows 设备名、重复路径、加密标志、大小/数量/压缩比、CRC 和 JSON 清单校验测试通过。

外部执行记录：20260911142540-851a175e 提供计算包模块，独立复核并修正后纳入。20260911143830-89817532 的测试运行受 shell 调用失败影响，恢复运行 20260911144509-8e2b306c 因越出允许范围的依赖证书文件被文件策略拦截；这两次补丁未应用。最终 API/AI 验收测试由本地重新编写并执行。

限制详见 README 和桌面指南：约 100 MiB 结果包；外部 GROMACS/WSL/驱动；WSL 取消延迟到当前步骤退出；无恢复租约或系统服务；安装器未签名。
